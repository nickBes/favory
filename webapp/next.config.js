module.exports = {
    images: {
        domains: ['i.creativecommons.org']
    },
    eslint: {
        // check with eslint before actually using it to check what error you get
        ignoreDuringBuilds: true,
    }
}